[package]
name = "km-control"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Pure control-theory primitives for the Kerbal flight controller (no I/O)."

[dependencies]
serde = { workspace = true }
